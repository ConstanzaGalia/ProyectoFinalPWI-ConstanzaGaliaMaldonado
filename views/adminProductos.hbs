<div class="container">
  <a href="/admin"><button class="btn btn-secondary my-4">Volver</button></a>
  <div class="row">
    <div class="col-md-6">
      <a href="/admin/categoria/create" class="btn btn-info my-4">Agregar Producto</a>
      <table class="table">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Nombre</th>
              <th>Acciones</th>
            </tr>
          </thead>
          <tbody>
            {{#each categorias}}
            <tr>
              <th scope="row"> {{id}} </th>
                <td> {{nombre}} </td>
                <td> {{descripcion}} </td>
                <td> {{nombreCategoria}} </td>
                <td>$ {{precio}} </td>
                <td><img src="/images/{{uid}}" alt="No hay img" class="table-img-size"></td>
                <td> 
                  <a href="/admin/productos/delete/{{id}}" class="btn btn-danger">
                    eliminar
                  </a>
                  <a href="/admin/productos/update/{{id}}" class="btn btn-warning">
                    editar
                  </a>
                </td>
              </tr>
            {{/each}}
          </tbody>
      </table>
    </div>
  </div>
</div>